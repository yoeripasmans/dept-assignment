import React from 'react';
import { MenuButton, MenuText, MenuIcon } from './styled';

const Menu = () => {
  return (
    <MenuButton>
      <MenuText>Menu</MenuText>
      <MenuIcon />
    </MenuButton>
  );
};

export default Menu;
